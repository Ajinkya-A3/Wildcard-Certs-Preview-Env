apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: wildcard-route
  namespace: default
spec:
  parentRefs:
    - name: wildcard-gateway
      namespace: gateway-system
  hostnames:
    - "test1.example.com"
    - "test2.example.com"
    - "api.example.com"
  rules:
    - backendRefs:
        - name: test-service
          port: 80